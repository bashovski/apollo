export default class UserPropertiesListitem {

    /**
     * @param type
     * @param address
     * @param price
     * @param status
     */
    constructor(type, address, price, status) {
        this.type = type;
        this.address = address;
        this.price = price;
        this.status = status;
    }
}
